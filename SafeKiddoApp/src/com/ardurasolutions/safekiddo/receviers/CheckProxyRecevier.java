package com.ardurasolutions.safekiddo.receviers;


import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

public class CheckProxyRecevier extends BroadcastReceiver {

	@Override
	public void onReceive(Context ctx, Intent arg1) {
		//if (Console.isEnabled())
		//	Console.logd("Proxy check recevier");
		//if (CommonUtils.isWiFiEnabled(ctx)) {
		//	ProxySystem.setupWiFiProxy(ctx);//.setWiFiProxyRes(ctx, "127.0.0.1", Constants.LOCAL_PROXY_PORT);
		//}
		//Config.getInstance(ctx).save(Config.KeyNames.LAST_PROXY_CHECK, System.currentTimeMillis());
	}

}
